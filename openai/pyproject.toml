[project]
name = "openai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai",
    "requests>=2.33.0",
]

[tool.uv.sources]
openai = { workspace = true }
